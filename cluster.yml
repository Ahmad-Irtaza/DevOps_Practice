apiVersion: v1
kind: Service
metadata:
  name: my-internal-service
spec:
  type: ClusterIP       # <--- Optional (it is the default)
  selector:
    app: nginx          # Finds the pods labeled "app: nginx"
  ports:
    - protocol: TCP
      port: 80          # Service port (internal)
      targetPort: 80    # Container port