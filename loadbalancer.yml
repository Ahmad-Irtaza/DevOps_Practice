apiVersion: v1
kind: Service
metadata:
  name: my-public-service
spec:
  type: LoadBalancer    # <--- Triggers AWS/GCP to give you an IP
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80          # The port the public will hit
      targetPort: 80    # Container port